# HabitForge — Habit Tracker & Daily Planner

Production-level freemium mobile app built with React Native (Expo).

## Build
```bash
npm install
npx expo start
eas build --platform android --profile production
```
